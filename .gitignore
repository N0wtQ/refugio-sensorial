node_modules/
dist/
.env
.env.local
.DS_Store
.claude/

# Generated at build time by scripts/fetch-logos.mjs
public/logos/

# Not tracked on purpose: CI runs `npm install` (not `npm ci`) and
# regenerates this fresh every build from package.json. A committed lock
# previously drifted out of sync and shipped a broken dependency version
# (see be65923).
package-lock.json
